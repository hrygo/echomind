# 🏗️ EchoMind 工程框架说明

**EchoMind** 是一个现代化的全栈AI驱动应用，采用前后端分离架构。

---

## 🔧 技术栈

| 层级 | 技术 | 作用 |
|------|------|------|
| **前端** | Next.js 16.0.3 + TypeScript | React全栈框架，SSR/SSG支持 |
| **状态管理** | Zustand | 轻量级状态管理 |
| **数据获取** | TanStack Query | 服务端状态管理 |
| **UI组件** | Radix UI + Tailwind CSS | 无头组件 + 原子化CSS |
| **后端** | Go 1.24 + Gin | 高性能Web框架 |
| **ORM** | GORM | Go对象关系映射 |
| **数据库** | PostgreSQL + pgvector | 主数据库 + 向量搜索 |
| **缓存** | Redis | 缓存和会话存储 |
| **任务队列** | Asynq | 异步任务处理 |
| **AI集成** | OpenAI + Gemini | AI服务提供商 |

---

## 📁 项目结构

```
echomind/
├── backend/                 # Go后端服务
│   ├── cmd/                # 应用入口点
│   ├── internal/           # 内部包
│   │   ├── app/           # 应用初始化和容器
│   │   ├── handler/       # HTTP处理器
│   │   ├── model/         # 数据模型
│   │   ├── service/       # 业务逻辑
│   │   ├── repository/    # 数据访问层
│   │   └── router/        # 路由配置
│   ├── configs/           # 配置文件
│   └── migrations/        # 数据库迁移
├── frontend/               # Next.js前端应用
│   ├── src/
│   │   ├── app/          # App Router页面
│   │   ├── components/   # React组件
│   │   ├── hooks/        # 自定义Hooks
│   │   ├── lib/          # 工具库
│   │   ├── store/        # 状态管理
│   │   └── types/        # TypeScript类型
├── deploy/                # 部署配置
├── scripts/              # CI/CD脚本
└── docs/                 # 项目文档
```

---

## 🔍 模块配置说明

### 后端工程
- **Go 模块名称**: `github.com/hrygo/echomind`
- **Go 版本**: 1.24.0
- **模块路径**: `backend/go.mod`
- **主要依赖**:
  - `github.com/gin-gonic/gin` - Web框架
  - `gorm.io/gorm` - ORM框架
  - `github.com/hibiken/asynq` - 任务队列
  - `github.com/sashabaranov/go-openai` - OpenAI集成
  - `github.com/google/generative-ai-go` - Gemini集成

### 前端工程
- **包名称**: `frontend`
- **版本**: 1.1.0
- **包管理器**: pnpm
- **主要依赖**: Next.js 16.0.3、React 19.2.0、TypeScript 5
- **模块路径**: `frontend/package.json`

---

## ⚙️ 核心服务

1. **API网关**: Gin框架提供RESTful API
2. **认证服务**: JWT-based身份认证
3. **邮件同步**: IMAP邮件自动同步和处理
4. **向量搜索**: 基于pgvector的语义搜索
5. **AI集成**: OpenAI/Gemini智能对话和生成
6. **任务队列**: Asynq异步任务处理
7. **实时通信**: WebSocket支持

---

## 🚀 开发工具链

- **构建**: Makefile统一构建命令
- **容器化**: Docker + Docker Compose
- **代码质量**: ESLint + TypeScript + Go tests
- **CI/CD**: GitHub Actions自动化
- **监控**: 结构化日志 + 健康检查

---

## 📋 快速启动

### 后端启动
```bash
make dev-db          # 启动数据库
make db-init         # 初始化数据库
make run-be          # 启动后端服务 (端口: 8080)
```

### 前端启动
```bash
cd frontend
pnpm install
pnpm dev             # 启动前端服务 (端口: 3000)
```

### 测试命令
```bash
make test           # 后端测试
make test-fe        # 前端测试
make build          # 后端构建
make build-fe       # 前端构建
```

---

这是一个面向企业级应用的现代化工程框架，具备高可扩展性、可维护性和完整的开发工具链支持。

- **后端**: Go模块 `github.com/hrygo/echomind`，提供高性能API服务
- **前端**: Next.js应用 `frontend` v1.1.0，提供现代化用户界面